version: '3.2'

services:
  vrddt-droplets:
    build: ./
    environment:
      - MONGO_URI=mongodb://mongo
      - LOG_LEVEL=info
    ports:
      - "8080:8080"
    links:
      - mongo
    networks:
      - vrddt_droplets_net
  mongo:
    image: mongo:3-stretch
    ports:
      - "27017:27017"
    networks:
      - vrddt_droplets_net

networks:
  vrddt_droplets_net:
